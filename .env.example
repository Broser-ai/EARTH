# EARTH adapter credentials — never commit real keys.
# Do not put secrets in VITE_* (they would ship to the browser).
# Live Roboflow / Tinker / Inkling clients attach from a sovereign backend.

ROBOFLOW_API_KEY=
TINKER_API_KEY=
INKLING_WEIGHTS_URI=
